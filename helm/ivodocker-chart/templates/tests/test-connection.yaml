apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ivodocker-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ivodocker-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ivodocker-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
