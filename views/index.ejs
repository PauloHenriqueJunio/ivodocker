<%- include('./layouts/header') %>

<article class="artc2">
    <h1>Postagens do blog, only tecnologias!</h1>
    <p>Abaixo estão as postagens feitas!</p>
    
        <% if (posts && posts.length > 0) { %>
        <ul style="list-style-type: none; padding: 0;">
            <% posts.forEach(post => { %>
                <li class="post-card">
                    <h3 class="adh"><%= post.titulo %></h3>
                    <p class="testelala"><%= post.conteudo.substring(0, 200) %>...</p>
                    <p style="font-size: 0.9em; color: #888;">
                        Categoria: <%= post.Categoria ? post.Categoria.nome : 'Sem categoria' %>
                    </p>
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>Nenhum post cadastrado ainda.</p>
    <% } %>
</article>

<%- include('./layouts/footer') %>