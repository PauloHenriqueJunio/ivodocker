<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atividade PWEB</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="shortcut icon" href="/imagens/favicon.ico" type="image/x-icon">
</head>
<body>
    <header>
        <h1 id="teste">Blog sobre tecnologia</h1>
        <p>Aquilo que vim em sua mente pode postar! Quanto mais informações melhor! <br><strong>Favor mantenha o respeito.</strong></p>
        <% if (typeof usuarioLogado !== 'undefined' && usuarioLogado) { %>
            <div class="welcome-message-header">Bem-vindo, <%= usuarioLogado.nome %>!</div>
        <% } %>
        <nav>
            <a href="/"><span>Home</span></a>
            <% if (typeof usuarioLogado !== 'undefined' && usuarioLogado) { %>
            <a href="/dados"><span>Gerenciar Dados</span></a><% } %>
            <a href="mailto:phjsl1@aluno.ifal.edu.br, gasn2@aluno.ifal.edu.br?subject=Contato%20do%20Site&body=Ol%C3%A1%2C%20gostaria%20de%20entrar%C2%A0em%20contato." target="_self"><span>Fale Conosco</span></a>
            <% if (typeof usuarioLogado !== 'undefined' && usuarioLogado) { %>
                <a href="/logout" style="margin-left:10px;"><span>Logout</span></a>
            <% } else { %>
                <a href="/login" style="margin-left:20px;"><span>Login</span></a>
            <% } %>
        </nav>
    </header>
    <main>
